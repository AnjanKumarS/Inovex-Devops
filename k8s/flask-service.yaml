apiVersion: v1
kind: Service
metadata:
  name: inovex-service
spec:
  type: NodePort
  selector:
    app: inovex-app
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30007  # Any unused port from 30000–32767
